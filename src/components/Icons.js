/*function Icon(props) {
  return (
    <div className='col-lg-4'>
      <img src={props.image} alt={props.title} className='bd-placeholder-img rounded-circle'></img>
      <h2>{props.title}</h2>
      <p>{props.paragraph}</p>
      <p>
        <a className='btn btn-secondary' href={props.link}>
          View details &raquo;
        </a>
      </p>
    </div>
  )
}

function AllIcons(props) {
  const data = props.data ?? []
  return (
    <>
      <div className='row'>
        {data.map((element, index) => (
          <Icon key={index} {...element} />
        ))}
      </div>
      <hr className='featurette-divider' />
    </>
  )
}

export default AllIcons*/
